version: '3.7'
services:
  php8-sf6:
    container_name: php8-sf6
    build: ./docker
    depends_on:
      - database
    ports:
      - "9000:8000"
    volumes:
      - /home/sfournage/cat-adoption:/var/www/html
  database:
    container_name: db-cat-adoption
    image: postgres:15
    environment:
      POSTGRES_DB: cat-adoption
      # You should definitely change the password in production
      POSTGRES_PASSWORD: admin
      #POSTGRES_USER: admin
    ports:
      - "5432:5432"
  pgadmin:
    container_name: data-admin
    image: adminer
    restart: always
    ports:
      - 5050:8080
